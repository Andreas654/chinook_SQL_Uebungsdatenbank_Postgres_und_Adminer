{
  "name": "PostgreSQL and Adminer",
  "dockerComposeFile": "docker-compose.yml",
  "service": "adminer",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker"
      ]
    }
  },
  "forwardPorts": [
    8080,
    5432
  ],
  //"postCreateCommand": "sleep 5 && docker-compose up -d"
  //"postCreateCommand": "./start.sh" 
  // Beide optionen funktionieren nicht
  // deswegen manueller start notwendig im Terminal im Ordner devcontainer mit ./start.sh
  // oder mit absolutem Pfad /workspaces/Test3/devcontainer/start.sh"
  "postCreateCommand": "./start.sh" 
}
